node_modules/
dist/
coverage/
*.log
.env
.env.*
.claude/settings.local.json

# discord-bot / extractor / gap-tracker のローカル設定(実 ID を含むためコミットしない。*.yaml.example のみ追跡)
apps/discord-bot/config/*.yaml
apps/extractor/config/*.yaml
apps/gap-tracker/config/*.yaml

# extractor のローカル clone(実 minutes / knowledge-base を含むためコミットしない)
.clones/

# 週次 golden eval の生成物(§10.2。CLI が書き出す)
evals/eval-result.json

# 抽出品質レビュー表(§10.3。実議事録の内容を含むためコミット禁止・ADR-0012)
evals/.review/
